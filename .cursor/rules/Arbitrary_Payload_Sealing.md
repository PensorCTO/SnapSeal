Arbitrary Payload Sealing PipelineDescription: Upgrades the FactLockCam capture pipeline to accept non-media MIME types while preserving Polygon anchoring, AES-GCM encryption, and Supabase E2EE storage.Instructions:Step 1: Execute supabase migration new to alter the sealed_assets table (or equivalent ledger) to accept a generic mime_type string instead of a strict media enum.Step 2: Enforce Supabase Row Level Security (RLS) limits on generalized file sizes to protect the "Sovereign Free Baseline" tier from abuse.  Step 3: Implement a generic file-picker service in Flutter that routes the binary through the existing local encryption class.Step 4: Update the Archive UI controllers to gracefully degrade rendering for unsupported MIME types, defaulting to a standard document icon when native thumbnails cannot be generated.Step 5: Verify the Polygon smart contract anchoring remains entirely agnostic to the underlying byte structure.